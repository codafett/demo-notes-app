{
  "name": "notes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "AWS_PROFILE=rjg sst test --testPathIgnorePatterns=/frontend/",
    "start": "AWS_PROFILE=rjg sst start",
    "build": "AWS_PROFILE=rjg sst build",
    "deploy": "AWS_PROFILE=rjg sst deploy",
    "remove": "AWS_PROFILE=rjg sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "0.69.2",
    "@serverless-stack/resources": "0.69.2",
    "aws-cdk-lib": "2.15.0",
    "aws-sdk": "^2.1099.0",
    "stripe": "^8.213.0",
    "uuid": "7.0.3"
  }
}
